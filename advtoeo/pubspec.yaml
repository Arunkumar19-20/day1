name: flutter_todo_app
description: Flutter Todo App with Spring Boot backend
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  cupertino_icons: ^1.0.2
  firebase_core: ^3.5.0

  firebase_auth: ^5.1.1
  cloud_firestore: ^5.2.0
  firebase_storage: ^12.1.0
  firebase_messaging: ^15.1.0
  firebase_crashlytics: ^4.1.0
  firebase_analytics: ^11.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
